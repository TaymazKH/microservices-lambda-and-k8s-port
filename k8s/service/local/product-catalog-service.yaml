apiVersion: v1
kind: Service
metadata:
  name: product-catalog-service
  labels:
    app: product-catalog-service
spec:
  type: NodePort
  selector:
    app: product-catalog-service
  ports:
    - name: http
      protocol: TCP
      port: 3550
      targetPort: 3550
      nodePort: 30550
