apiVersion: v1
kind: Service
metadata:
  name: ad-service
  labels:
    app: ad-service
spec:
  type: NodePort
  selector:
    app: ad-service
  ports:
    - name: http
      protocol: TCP
      port: 9555
      targetPort: 9555
      nodePort: 30555
