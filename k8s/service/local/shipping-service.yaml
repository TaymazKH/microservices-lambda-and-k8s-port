apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  labels:
    app: shipping-service
spec:
  type: NodePort
  selector:
    app: shipping-service
  ports:
    - name: http
      protocol: TCP
      port: 50053
      targetPort: 50053
      nodePort: 30053
