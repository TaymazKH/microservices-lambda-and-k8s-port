apiVersion: v1
kind: Service
metadata:
  name: currency-service
  labels:
    app: currency-service
spec:
  type: NodePort
  selector:
    app: currency-service
  ports:
    - name: http
      protocol: TCP
      port: 7000
      targetPort: 7000
      nodePort: 30000
